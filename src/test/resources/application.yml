spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:account_center_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql

account:
  store:
    type: jdbc

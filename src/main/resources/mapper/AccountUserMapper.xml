<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hypers.account.mapper.AccountUserMapper">

    <resultMap id="userResultMap" type="com.hypers.account.app.AccountUser">
        <id property="id" column="id"/>
        <result property="account" column="account"/>
        <result property="email" column="email"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="createdBy" column="created_by"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert">
        insert into account_users (id, account, email, name, phone, status, created_by)
        values (#{user.id}, #{user.account}, #{user.email}, #{user.name}, #{user.phone}, #{user.status}, #{user.createdBy})
    </insert>

    <update id="update">
        update account_users
        set account = #{user.account},
            email = #{user.email},
            name = #{user.name},
            phone = #{user.phone},
            updated_by = #{user.updatedBy},
            updated_at = current_timestamp
        where id = #{user.id}
    </update>

    <select id="selectById" resultMap="userResultMap">
        select id, account, email, name, phone, status, created_by, updated_by, created_at, updated_at
        from account_users where id = #{id}
    </select>

    <select id="selectByAccount" resultMap="userResultMap">
        select id, account, email, name, phone, status, created_by, updated_by, created_at, updated_at
        from account_users where account = #{account}
    </select>

    <select id="selectByKeyword" resultMap="userResultMap">
        select id, account, email, name, phone, status, created_by, updated_by, created_at, updated_at
        from account_users
        <where>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                and (account like concat('%', #{keyword}, '%')
                    or name like concat('%', #{keyword}, '%')
                    or email like concat('%', #{keyword}, '%')
                    or phone like concat('%', #{keyword}, '%'))
            </if>
        </where>
        order by created_at desc
    </select>

    <update id="updateStatus">
        update account_users set status = #{status}, updated_at = current_timestamp where id = #{id}
    </update>

</mapper>

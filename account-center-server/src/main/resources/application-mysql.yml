spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${ACCOUNT_DB_URL:jdbc:mysql://localhost:3306/account_center?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${ACCOUNT_DB_USERNAME:account}
    password: ${ACCOUNT_DB_PASSWORD:account}
